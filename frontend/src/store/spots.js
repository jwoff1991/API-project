const initialState = {
    allSpots: {
        //normalized kvps
    },
    singleSpot: {
        //flattened db info
    }
}


export default spotReducer(state, action){
    switch(action.type){
        default:
        return state
    }
}
